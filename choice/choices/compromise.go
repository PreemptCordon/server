package choicetypes

func CompromiseRCVEval(decision Decision)  DecisionResult {
	options := decision.options
	seats = decision.seats
	runningtotal = string[int]
	// initialize starting preference
	for (vote in votes){
		runningtotal[vote.preference[0]] += 1
	}
	
}